API_URL=http://localhost:3001

# Used to build the exact OAuth redirect_uri (must match what's registered in the Meta App).
# In production this is your Vercel deployment's public URL.
APP_URL=http://localhost:3000

# Public Meta App ID (not secret) - used to build the "Conectar com Facebook" OAuth dialog URL.
META_APP_ID=
